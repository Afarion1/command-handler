package com.github.afarion1.command_handler.command.config;

public enum CommandListType {
    /**
     * Listed commands will be visible for Command List, unlisted will not
     */
    LISTED, UNLISTED
}
